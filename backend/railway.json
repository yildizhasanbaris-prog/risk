{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install && npm run build"
  },
  "deploy": {
    "startCommand": "echo '=== Migration ===' && npx prisma migrate deploy && echo '=== Starting Node ===' && ls -la dist/index.js && node dist/index.js"
  }
}
